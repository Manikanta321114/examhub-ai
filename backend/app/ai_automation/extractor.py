# Extractor helper for parsing raw text and PDFs
# Currently placeholder for future logic connecting to OCR or LLM vision models.

class AIExtractor:
    @staticmethod
    def extract_from_text(raw_text: str) -> dict:
        """
        Takes raw text from scanned website and extracts structured exam fields.
        """
        # Placeholder returning raw parsed dictionary
        return {}
